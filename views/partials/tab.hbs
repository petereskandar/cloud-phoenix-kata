<div class="card">
  <div class="card-body">
    <div class="col-md-10"><h5 class="card-title"><strong>Api Endpoint:</strong> {{endpoint}}</h5></div>
    <div class="col-md-12">
      {{#if tryEndpoint}}  
        <a class="btn btn-primary btn-lg float-right" href="{{tryEndpoint}}" target="_blank" role="button">Try</a>
        {{#generatecert}}
            {{cert}}
        {{/generatecert}}
      {{else}} 
        <a class="btn btn-primary btn-lg float-right" href="{{endpoint}}" target="_blank" role="button">Try</a> 
      {{/if}}  
    </div> 
  </div>
</div>